# 🧪 Тестирование и примеры использования

## Быстрый старт

### 1. Регистрация тестовых пользователей

Перейдите на http://localhost:8000/register/ и создайте пользователей:

**Ученик:**
- Имя: `Иван Иванов`
- Email: `ivan@mail.ru`
- Пароль: `Password123`
- Роль: Ученик

**Повар:**
- Имя: `Мария Петрова`
- Email: `maria@mail.ru`
- Пароль: `Chef12345`
- Роль: Повар

**Администратор:**
- Имя: `Сергей Сидоров`
- Email: `admin@mail.ru`
- Пароль: `Admin12345`
- Роль: Администратор

### 2. Добавление меню

Повар может добавить меню через http://localhost:8000/menu/add/item/

**Завтрак:**
- Первое блюдо: Каша гречневая
- Напиток: Молоко
- Калории: 250
- Цена: 25₽

**Обед:**
- Суп: Борщ украинский
- Основное: Котлета мясная
- Салат: Винегрет
- Напиток: Компот
- Калории: 550
- Цена: 50₽

### 3. Добавление продуктов (через админ панель)

http://localhost:8000/admin/ -> MenuService -> Products

```
Название: Молоко
Количество: 500
Цена за единицу: 45
Минимальный уровень: 200
```

### 4. Тест функций

#### Для ученика:
1. Войти как ученик
2. Перейти в профиль `/profile/`
3. Добавить аллергию (например: "Молоко")
4. Перейти на `/payment/` и пополнить баланс (например: 500₽)
5. Перейти на `/order/` и заказать питание
6. Посмотреть заказы на `/my-orders/`

#### Для повара:
1. Войти как повар
2. Перейти на `/chef/dashboard/`
3. Просмотреть заказы на сегодня
4. Отметить заказы как полученные
5. Перейти на `/chef/inventory/` и создать заявку на закупку

#### Для администратора:
1. Войти как администратор
2. Перейти на `/admin/dashboard/`
3. Просмотреть статистику
4. Перейти на `/admin/purchases/` и одобрить заявки
5. Перейти на `/admin/statistics/` и просмотреть отчеты

## 🐍 Django Shell команды

```bash
python manage.py shell
```

### Создание пользователя программно

```python
from UserService.models import User

user = User.objects.create(
    name="Test User",
    email="test@mail.com",
    password="securepass123",
    role="student",
    balance=0
)
```

### Создание завтрака

```python
from MenuService.models import Breakfast

breakfast = Breakfast.objects.create(
    first_dish="Омлет из трех яиц",
    drink="Какао",
    calories=350,
    price=30,
    contains_allergens="Яйца, молоко"
)
```

### Создание обеда

```python
from MenuService.models import Lunch

lunch = Lunch.objects.create(
    soup="Суп из курицы",
    main="Рыба жареная",
    salad="Салат овощной",
    drink="Компот",
    calories=600,
    price=60,
    contains_allergens="Рыба"
)
```

### Создание дневного меню

```python
from MenuService.models import DailyMenu

menu = DailyMenu.objects.create(
    week_day="Понедельник",
    breakfast=breakfast,
    lunch=lunch
)
```

### Создание заказа

```python
from OrderService.models import Order

order = Order.objects.create(
    user=user,
    meal_type="breakfast",
    breakfast=breakfast,
    status="ordered"
)
```

### Создание платежа

```python
from OrderService.models import Payment

payment = Payment.objects.create(
    user=user,
    amount=500,
    payment_type="single",
    status="paid",
    receipt_number="RCP-20260211120000-1"
)

# Обновить баланс пользователя
user.balance += 500
user.save()
```

### Создание заявки на закупку

```python
from OrderService.models import PurchaseRequest

request = PurchaseRequest.objects.create(
    created_by=chef_user,
    product_name="Помидоры",
    quantity=1000,
    unit="г",
    reason="Низкие остатки для салатов",
    status="pending"
)
```

### Просмотр статистики

```python
from OrderService.models import Order, Payment
from django.db.models import Count, Sum

# Всё заказы
all_orders = Order.objects.all()
print(f"Всего заказов: {all_orders.count()}")

# Платежи по типам
payments = Payment.objects.values('payment_type').annotate(
    count=Count('id'),
    total=Sum('amount')
)

# Самый популярный обед
popular_lunch = Order.objects.filter(
    meal_type='lunch'
).exclude(lunch=None).values('lunch__main').annotate(
    count=Count('id')
).order_by('-count')
```

## 📊 Тестовые сценарии

### Сценарий 1: Полный цикл ученика

1. Регистрация → ✅
2. Добавление аллергии → ✅
3. Просмотр меню → ✅
4. Пополнение баланса → ✅
5. Заказ питания → ✅
6. Просмотр заказа → ✅
7. Оставление отзыва → ✅

### Сценарий 2: Управление столовой

1. Повар создает заявку на закупку → ✅
2. Администратор одобряет заявку → ✅
3. Администратор просматривает отчет → ✅
4. Повар управляет остатками → ✅

### Сценарий 3: Распределение заказов

1. Ученик делает заказ на завтрак → ✅
2. Ученик делает заказ на обед → ✅
3. Повар видит заказы в панели → ✅
4. Повар отмечает заказ как выданный → ✅
5. Ученик видит статус "Получено" → ✅

## 🐛 Отладка

### Просмотр логов

```bash
# Включить подробный вывод
python manage.py runserver --verbosity 3
```

### Проверка SQL запросов

```python
from django.db import connection
from django.test.utils import CaptureQueriesContext

with CaptureQueriesContext(connection) as context:
    # Ваш код
    orders = Order.objects.all()
    
print(f"Выполнено запросов: {len(context)}")
for query in context:
    print(query['sql'])
```

## ✅ Проверочный список администратора

- [ ] Все модели зарегистрированы в админ панели
- [ ] Можно создавать/редактировать меню
- [ ] Можно просматривать заказы
- [ ] Можно одобрять заявки
- [ ] Статистика отображается правильно
- [ ] Пользователи разных ролей видят правильные страницы
- [ ] Система платежей работает
- [ ] Управление аллергиями работает

---

**Успешное тестирование = Готовое приложение!**
